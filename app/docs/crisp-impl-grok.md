# 清晰感实现取舍（grok）

对照 r3 与分类表。未做奇数尺寸归格。

A：tertiary #6d6d6d、muted #8f8f8f；深色 #969696 / #787878。占位/禁用/装饰留 muted；说明升 tertiary；完成态内容与会话标题升 secondary/ink。`--icon` 只给导航/工具栏；chevron/spinner 走 tertiary。

B：次级按钮与 chip 补 `border-line`；深色去影改 line-highlight。侧栏选中只改局部白 pill，不动全局 fill-active。

C2-lite：`--tint` 只接侧栏选中字/标、`:focus-visible` ring、Switch on。主 CTA 仍近黑。

r3：行锁 13/medium + 11/regular；接缝 line-strong；pill 补 hover/pressed/折叠 selected，150ms ease-out；深色用 raised，不抄白 pill。
