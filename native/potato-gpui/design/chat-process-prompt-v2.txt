Edit the attached Potato chat design sheet according to four exact user annotations. Keep the same borderless native white/gray style, Chinese typography, black text, compact line icons, and 2-by-2 specimen layout. Target dimensions1536x1200. This is a focused revision of ONE selected direction. Preserve reply content and overall hierarchy, do not invent new features. Reference is the previous design sheet, not a live app. Make it lighter and less explanatory.
1. Simplify completed summary: replace BOTH '已完成 · 2分56秒 · 8项操作' with only '耗时 2分56秒'. No operation count, no additional completion words. Use one small chevron before it: DOWN when collapsed, UP when expanded, consistent for all disclosure controls. Whole row remains clickable. The complete answer follows summary immediately with20px gap, not a large blank gap.
2. A failed tool call is normal during agent work. Replace entire bottom-right alarm/error presentation with an ordinary ongoing process snippet. Header small spinner '正在验证 · 1分24秒' and UP chevron. Below one dark sentence '正在尝试其他测试命令。'. Three compact operation rows: a small RED terminal/error icon and gray text '运行界面测试' with DOWN chevron (closed details); then normal gray terminal icon and gray text '检查测试配置' with DOWN chevron; then spinner and normal gray text '重新运行测试…'. ONLY the single failed row icon is red. No red words, no big warning triangle, no failure count, no failure heading, no '执行未完成', no error explanation paragraph, no forced open error output, no final answer or final copy/refresh buttons in this ongoing specimen. Failed details remain discoverable via the row chevron, not automatically expanded.
3. Remove the bottom-left text link '收起过程' completely, close its space. Top UP chevron collapses this process. Final answer directly follows last expanded tool output by20px. Never add a redundant collapse link at bottom.
4. Remove the redundant labels '展开过程' AND other outside state labels '进行中','完成 · 自动收起','异常 · 保持可见'. Let actual state row and arrow express the state. Remove explanatory hover illustration and its long small print text entirely. Remove footer instruction text and date. Keep only small heading '轻量过程行' top-left, no subtitle. Four examples should be separated by generous whitespace only, never boxed.
Top-left active remains small spinner '正在检查 · 1分24秒' with UP chevron, one narration '正在核对会话切换和草稿保留。', three compact operation rows '读取 chat.rs', '修改 view.rs  +7 −2', spinner '运行界面测试…'. Small subdued link at bottom '更早记录'. Closed tool detail rows use DOWN chevrons.
Top-right completed remains DOWN chevron '耗时 2分56秒' then final text '已完成这一轮检查。' and '草稿保留和会话切换问题已修复，验证通过。', then small copy and clockwise circular-arrow regenerate icons. No background or circular border around default icons.
Bottom-left expanded remains UP chevron '耗时 2分56秒', narration '先核对消息分组，再验证折叠行为。', '读取 chat.rs' DOWN, '修改 view.rs  +7 −2' DOWN, '运行界面测试' UP, a flat faint gray two-line code detail 'cargo test -p potato-gpui' and '23 passed · 0 failed'. Then20px gap followed by same final answer with copy and clockwise circular-arrow icons. NO bottom collapse link.
Maintain realistic readable14–16px equivalent body text,16px icons with invisible32px hit targets, compact28–32px tool rows. No decoration, shadows, timeline, extra cards or labels. Neutral #1d1d1f main text, #6b6b6b secondary, #fff background, #f5f5f5 code inset. No general warning UI. No footer or date. Do not put 4 ideas in one image; these are 4 states/examples of the same component.
