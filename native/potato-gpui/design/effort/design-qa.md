# 2026-09-07 用户反馈后的复核

final result: passed（本轮修正后的滑杆局部）

此前通过判断过于宽松：未识别填充区左侧留白、轨道上下双层灰底和底部重置行破坏紧凑布局的问题。以下新证据取代旧版深度弹窗的视觉通过结论。

- 填充从轨道实际左边界开始，显式保留左上/左下 9px 圆角；档位的 14px 内缩只用于定位，不再成为填充留白。
- 轨道和滑块均为 28px 高，移除轨道竖向 padding，滑块宽度改为 22px。
- 重置功能移到右上角带可访问名称和提示的小图标，去掉独立底部操作行；字体和左右留白统一。
- 修复单档时内层轨道/滑块未禁用的问题。
- 实际窗口：actual-effort-polished.png；未缩放局部裁切：actual-effort-polished-detail.png。局部复核确认左边填充贴边且圆角完整，没有原来的灰色内框空隙。
- 构建通过；已有原生鼠标映射回归测试用于检查新的轨道尺寸。
- 未替换已安装应用；保留隔离预览。

---

# 独立模型与思考深度入口

final result: passed

范围：2026-09-06 用户确认的修订稿 selected.png；原生 GPUI 实现。未改造为网页。

## 视觉核验

在同一图像工具输出中打开 selected.png、actual-effort.png 与 actual-model.png 做组件层级对照。参考图是放大展示的两个状态；实际图为 1080×760 原生窗口，因此比较组件的结构、相对间距与状态，不将整幅画布尺寸当作相同倍率。

- 两个独立入口；当前展开项浅灰底，另一项透明。模型菜单右侧勾选；深度菜单显示当前值、两端说明和灰阶离散滑杆。
- 系统字体、中性灰主题、细边框和轻阴影与现有产品一致。未新增位图装饰或替换原版输入框布局。
- 保留现有服务商分组、管理入口和左侧模型箭头，属于原生产品的既有信息结构；与稿件相比是 P3 差异。
- 增加“恢复默认”以明确撤销显式档位；默认意味着请求不携带 effort，由服务商决定。不会把默认伪装成某一档。
- 未发现阻止本次组件交付的 P0/P1/P2 视觉问题。深色窗口未截图核验，使用既有主题 token。

## 行为与能力边界

- 可选档位优先使用模型元数据，再继承服务商元数据；显式空数组表示不支持。预算类 thinking 参数不转成 effort 滑杆。能力未知时不猜测通用档位。
- 模型发现保留能力元数据，已有用户配置优先；设置支持按从浅到深的顺序填写可选档位。
- 偏好保存在对应服务商的对应模型中，切换模型不传递其他模型的偏好。已过期的档位在请求阶段省略，回到服务商默认。
- 保存过程中禁用调节和模型切换，阻止提前发送；保存失败保留原配置并提示错误。
- 提供键盘方向键、Home、End 和恢复默认按钮。

## 验证证据

- 核心能力单测：2 项通过（覆盖优先级、空集合、预算模式、去重、失效值和发现元数据）。
- 真实本地 API 集成测试：1 项通过（非法值拒绝、持久化、模型间隔离、清空恢复默认）。
- GPUI 渲染及鼠标事件测试：1 项通过（按服务商区分同名模型、三档范围、点击末档映射）。
- Debug 构建通过。
- 实际窗口截图使用 /tmp 独立数据中的预设弹窗状态。CUA 点击多次返回 noWindowsAvailable，不能声称通过系统鼠标完成端到端流程；真实控件点击由 GPUI 测试验证。
- 未向真实模型发送计费请求，未替换 /Applications/Potato.app。

全目标 Clippy 当前被其他改动中的 src/media.rs:78（测试模块后还有函数）阻塞；本次未改动该文件。
主程序目标 Clippy（--bin potato-gpui -- -D warnings）通过；最终 Debug 构建通过。

## 2026-09-07 右端与拖动复核

轨道映射内缩从 14px 改为 11px，与 22px 滑块半宽一致，最高档位右端不再残留 3px 背景。拖动绘制使用连续 percentage 坐标，档位标签仍按 step 取整；松手后立即吸附并提交一次保存。回归测试验证 0.63 的连续位置不会被绘制为 0.5。原生最高档位截图 actual-ultra-flush.png 与局部 actual-ultra-flush-detail.png 确认右端齐平。构建、原生鼠标事件回归通过；未宣称系统级拖动帧率已测量。
