# 对齐原前端的验收记录

视觉基准为本机 `/Applications/Potato.app` 的首页与会话页，行为同时参考 `app/` React 实现。
原前端保持原样，Rust UI 在独立目录推进。优先级为行为和显示正确、视觉接近，再压缩体积。

| 项目 | 本阶段结果 | 仍需完成 |
| --- | --- | --- |
| 侧栏 | 264 px，原导航顺序，折叠、搜索、品牌图标 | 时间标签、分组、右键菜单、置顶和归档 |
| 首页 | 原问候文案，34 px 字号、42 px 行高；全屏截图中标题与输入卡纵向位置已对齐 | 字体字距细节 |
| 输入卡 | 768 px 最大宽度，20 px 圆角，多行输入，底部工具行；调整边框、阴影和禁用发送按钮 | 自适应高度、附件、模型与工作区选择、语音 |
| 会话 | 用户右侧灰气泡，助手左侧正文，输入卡移到底部 | Markdown、复制、重新生成、工具与审批卡片 |
| 主题 | 原中性色系；浅色和深色经过本机截图检查 | 完整 hover、focus、disabled 状态对照 |
| 中文 | 中文显示、多行粘贴经过本机检查 | 拼音输入法候选/组合输入及 Windows 字体回退 |
| 快捷键 | Enter 发送，Shift+Enter 换行；真实控件键盘事件测试覆盖 | 真实输入法候选上屏防误发验收，以及其余全局快捷键 |
| 系统集成 | macOS 透明融合标题栏，保留原生红绿灯；全屏显示顶部品牌，普通窗口隐藏；收起侧栏按钮左侧避让 92 px | 菜单、无障碍语义树、签名和更新，Windows 实机验收 |
| 其他页面 | 导航位置预留，点击明确提示未迁移 | 定时、技能、记忆和完整设置页面 |

本机确认：窗口启动、首页渲染、中文多行粘贴、演示发送、用户气泡与助手正文、
深色主题。后续已接入 HTTP/SSE 文字聊天，完成本地测试服务的发送、续聊、停止、重连 UI 验收；
尚未对用户真实模型供应商做完整集成验收。功能现状以 `MIGRATION_STATUS.md` 为准。
macOS/Windows CI 已配置，Windows 执行结果尚未取得。

## 本轮首页修正

已安装版本与当前工作区源码有视觉参数差异。本轮按用户认可的截图基准，读取已安装包确认：
标题字号 34 px / 行高 42 px、标题下 margin 64 px，加输入卡外层 top padding 12 px；
首页 bottom padding 16vh，输入卡外层 bottom padding 24 px，卡片圆角 20 px。
当前工作区 `app/` 是另一组 32 px / 28 px / 10vh / 16 px 参数，未修改这些原文件。

已同步画布 #fbfbfb、侧栏 #f0f0ee、输入卡白色、边框 #dcdcd9 和侧栏选中背景。
Iced 单层阴影近似原 CSS 的两层阴影；字体字距仍非像素级一致。
默认演示提示移出常驻底栏，模型位置仍明确显示「本地演示」；错误、连接状态照常显示。
不会为了视觉一致伪造历史会话或真实模型状态。

普通窗口、收起侧栏、全屏截图保存在 `measurements/home-fixed/`。
macOS 全屏快捷键为 Ctrl+Cmd+F，其他平台为 F11；模式随窗口事件同步，避免按尺寸猜测全屏状态。

## 首次测量

2026-09-06，本机 macOS ARM64，release / wgpu，空白首页：

- GPU 版本可执行文件约 9.90 MB（不含后端，不等于最终安装包体积）。
- 一次 `ps` 采样为 RSS 104640 KiB，约 102.2 MiB，CPU 0.0%。
- 没有测量首帧延迟、长会话滚动分位数、流式更新和 GPU 内存。
- 软件渲染早期样本较小，但在 Retina 屏幕上图标坐标/缩放异常；改为默认 GPU 后截图确认修复。

这些是原型观察值，不能用于宣称已经比原版更快。后续必须使用相同会话、窗口尺寸、
屏幕缩放和操作步骤对比，再决定是否值得替代现有前端。


## 2026-09-06 富文本与交互卡片

新增 `measurements/rich-chat/markdown-tools.png`、`approval.png`、`question.png`（均为本地模拟数据）。
采用原有主题的白色/深色卡片、16px 圆角和细边框。中文 Markdown 字体指定为平台中文字体；
CJK 斜体字面缺失时回退常规字体，避免异常字形。代码与工具输出启用高级 shaping。
macOS ARM64 release 可执行文件为 10,462,240 bytes，未包含后端；未测整体长会话性能。
当前还未实现代码高亮、表格布局、工具折叠和原版所有细节，不能视为像素级对齐完成。

## 2026-09-06 第二轮聊天样式 review

用户指出“复制原文”置顶、底部“3 条消息”等信息与原版不同。本轮重新打开安装版原生窗口，
检查普通回答和工具执行记录，同时核对安装包中的 `MessageActions` 与 `Markdown` 样式。
证据保存在 `measurements/style-review/`，原版截图为当前真实会话，Rust 截图为固定模拟数据。

修正：
- 回答底部使用原版 Lucide Copy 14px 图标，悬停才显示，预留位置避免布局跳动。
- 正常历史加载和完成不展示消息数量、已完成等页脚，恢复操作仅在断流/后台未确认时出现。
- 取消聊天标题额外文本，聊天输入栏不显示首页才有的项目入口，底边距恢复 24px。
- h1/h2/h3 调整至 20/18/16px 半粗；正文行高 28px。
- 行内代码使用浅灰背景和正常文字色；代码块使用 6px 圆角、12px 等宽文字、24px 行高，取消自创标题和边框。
- 工具记录默认折叠，展开再显示参数与输出；不再常驻大卡片和“已完成”标签。

范围限制：工具记录仍未组合为原版的步骤/耗时执行轨迹；重新生成、语法高亮未迁移。
Iced 原生画布还未提供与原版 DOM 等价的无障碍语义；本次截图仅验证可见布局，不代表无障碍或 Windows 验收。


## 日常聊天续轮（2026-09-06）

重新核对安装版窗口和原版 MessageActions/Markdown 源码。新增末轮重新生成使用同一 Lucide
14px 图标、悬停显示并保留高度；用户复制/编辑入口放在气泡外的悬停操作行。
保留原版代码块无标题、6px 圆角、12px 字号和 24px 行高，加入浅色适配的语法高亮。
表格支持表头、底部分隔线、列对齐和横向滚动；引用竖线按内容高度绘制，包含嵌套引用。

实际 macOS 测试使用 `/private/tmp/potato-chat-parity-0906` 独立数据库及回环模拟模型：
切换会话后恢复中文草稿；编辑回填原问题，取消恢复旧草稿；重新生成在当前会话追加问题与回答，
原消息和草稿均保留。窗口检查发现并修正引用竖线缺失、表头额外背景和重复编辑状态栏。
不把截图检查等同于像素级完全一致；原版工具轨迹、预览、复制反馈、IME 与完整输入行为仍待补齐。

最终包重新启动后，已检查浅色和深色表格、引用及嵌套竖线、代码高亮；测试窗口和模拟服务已关闭。


## 设置页重做（2026-09-06）

用户指出全屏长表单严重偏离原版。按 SettingsView 源码和原版 AX 中的分类/列表/详情层级重做：
居中 992×704 上限面板、208px 分类列、固定标题和关闭按钮、内容独立滚动、灰色遮罩保留聊天背景。
字段改为左标签右控件，使用 13px 字号、256px 输入宽度，服务商连接/模型分组，所有导航都带草稿保护。
详情沿用“服务商列表 / API key / Base URL / 保存连接 / 发现模型 / 模型 ID / 显示名”等原版术语。
目前 Iced 遮罩为半透明变暗，没有原版的背景模糊；不宣称像素级完全一致。模型行“使用”是原生选用入口，
原版更多高级设置的缺口保留在 MIGRATION_STATUS。

实际 macOS 窗口已通过 `⌘ ,` 展示新的模型与服务商面板，核对居中尺寸、分类列、列表分组、
标题和关闭图标。CUA 坐标点击持续返回 noWindowsAvailable，独立标识 QA 包和正常应用启动后仍出现；
详情页逐项点击、小窗口/深色及关闭键的完整视觉结果本轮未完成验证。不要将结构截图核对写成完整交互验收。
