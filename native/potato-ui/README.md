# Potato Rust native client

Iced 原生界面直接链接 `../potato-core`。窗口、SQLite 会话、模型设置与工具执行属于同一应用，
不需要 Python、Tauri、WebView 或独立 HTTP 后端。不再提供后端地址、令牌或模拟聊天模式。
`app/` 与 `console/` 的 Tauri 客户端仍保留；本目录尚未替换正式分发入口。

## 构建和运行

```sh
cargo run --locked --release
```

`cargo build --locked --release` 后执行 `python3 package.py` 打包 macOS 应用，Windows 用
`python package.py`。Python 仅为构建时文件打包工具，不放入应用。macOS 包包含麦克风用途说明；
它使用独立的应用标识 `dev.potato.native-preview` 和名称 **Potato Native**，具有本地 ad-hoc 签名；
未使用发布证书、未公证，不覆盖已安装的 Potato。

默认数据目录是系统本地应用数据目录下的 `dev.potato.rust-ui/native-v1`。可用
`POTATO_NATIVE_DATA_DIR` 指向临时目录进行测试。不会自动读取或修改旧版账号数据。
设置页可显式导入旧数据目录和密钥目录中的供应商、豆包及图片连接。

## 一体化启动

Iced 初始状态构造时直接打开 Rust 核心，并读取本地会话、当前模型和权限配置，首个界面状态
已包含这些数据。启动不探测端口、不连接模型、不扫描插件、不打开麦克风。
初始化失败显示本地存储错误，不能退回模拟回复。首次使用需要在设置中填写供应商。
后台任务由进程内执行器管理，应用退出后不会留存独立后端；定时任务只能在应用运行时执行。

```sh
POTATO_NATIVE_DATA_DIR=/absolute/disposable/path target/release/potato-ui --startup-smoke
```

该检查报告核心和初始界面状态准备时间，不代表点击图标到屏幕首帧的耗时。

## 当前可用链路

- 本地会话/历史、中文流式聊天、停止和重新附着运行任务。
- DeepSeek/sub2api 及已导入供应商配置、模型选择、Chat/Responses 协议。
- 核心独立审批列表、逐次允许/拒绝、用户问题与答案。
- 豆包语音设置；点击麦克风后才连接服务并打开系统默认输入设备，PCM 转为 16 kHz 单声道，
  再次点击结束录音。识别文字填回草稿，不自动发送；最长录音 180 秒。
- 图片和文档附件选择；PDF/Office 文本提取使用 Rust 核心。生成/编辑后的图片显示和保存。
- 会话重命名、置顶、归档/恢复、删除确认、标题与正文搜索。
- 定时任务创建/编辑、每天/单次/Cron、启停和手动执行、执行记录。
- 技能创建/编辑、ZIP 导入、启停与删除；记忆与角色文档编辑、保存冲突保护。
- 侧栏、主题、窗口尺寸和选中会话恢复；基础 Markdown、工具卡片及内容复制。

## 验证与限制

34 项 UI/适配器测试通过，包括无后端服务时的初始化、配置重开、模拟模型端点的完整流式
会话和落盘、立体声 48 kHz 转单声道 16 kHz。核心另有 46 项测试通过。Clippy 严格检查通过。
真实豆包麦克风与 sub2api 服务未验收，Windows 实机/安装包未验收。

已补齐编辑重发、末轮重新生成、运行期会话草稿与附件隔离、表格/引用和代码高亮。
尚未补齐工具轨迹/回合归组、IME 与原版输入快捷键、远程图片/文件预览、数学、完整设置、项目与改动侧栏、
托盘/自动更新、长列表虚拟化及完整无障碍。电脑操作驱动尚未在此客户端打包。
细项见 [功能对齐清单](MIGRATION_STATUS.md)。
这些缺口意味着它仍是开发预览，不是原 Tauri 界面的完全等价替代。

`scripts/mock_backend.py` 是旧 HTTP 原型的历史测试夹具；当前客户端不连接该服务。

## 本地包验证（2026-09-06）

macOS ARM64 优化构建已完成，`dist/Potato Native.app` 使用临时空数据目录启动。
首屏聊天输入和模型/语音设置页面已用实际窗口检查。启动状态检查报告约 44 ms / 21 ms
（两次打开同一个临时目录；不是整窗首帧测量）。应用没有子进程或 TCP 监听端口。
生成 `dist/Potato-Native-macOS-arm64.zip`，未发布、未安装覆盖原客户端。

### 功能对齐验收补充

本轮 34 项 UI 测试、46 项核心测试、两 crate 的严格 Clippy 与格式检查通过。
最新版 macOS ARM64 包用独立临时目录验证：记忆创建/保存/重读、草稿切页保护、
技能创建、提醒创建/手动执行/自动刷新聊天、会话置顶且保留当前消息。
启动状态准备约 85 ms（空临时目录一次测量，不是首帧指标）；未发现 TCP 监听端口。
测试窗口已关闭；新页面视觉仍较基础，复杂内容与完整平台验收见功能清单。
