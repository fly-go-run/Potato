# 第二版原生 iOS 审批面板验收

final result: passed

选定来源：第二个实际显示的 ImageGen 结果，`selected-reference.png`，853×1844，设计逻辑尺寸约390×844。
实现：`selected-implementation.png`，1179×2556，iPhone 17 393×852pt @3x。按逻辑宽度与面板区域归一比较；真实 iOS 状态栏、系统面板边距保留，参考图未包含系统状态栏。整图与面板均直接查看；无需放大检查素材，均为原生文字和 SF Symbols。
状态：目录审批、仅本次选中、详情收起。fixture 的目录名与任务名不同于设计图，为合成服务的真实返回值；背景会话保持现有产品实现。

- 字体：SF/PingFang 系统字，标题20pt，正文/范围17pt，路径及说明使用footnote。保留设计的标题/目录/辅助文字层次；目录路径可选择、可换行。
- 布局：默认470pt面板，24pt边距，关闭按钮在右上，目录图标与名称/路径左对齐，三个范围直接可见，说明在范围下方，详情折叠，底部固定拒绝与黑色主按钮。相较生成图的OS无关画面，实际面板略紧凑，符合前文“矮一些”的要求。
- 颜色：沿用Palette暖白/近黑/灰，选中范围黑底白字，拒绝为红色，分隔线弱化，无嵌套卡片。
- 图像：无插画素材；使用原生SF Symbols文件夹与关闭图标，未引入栅格界面或第三方标识。
- 文案：目录权限明确为读取/列举/搜索及是否递归；范围改变同步更新说明与提交按钮。电脑不声明能力时不显示持续授权。审批原因/参数放入详情，沙箱外执行提示保留在主内容。

迭代：首轮3项UI通过，但截图发现拒绝颜色被父视图覆盖，以及最大辅助字号下固定区按钮截断。已显式恢复红色，固定标题/操作区字号上限设为xxxLarge，正文继续支持辅助字号和滚动。最终`selected-large.png`确认状态和按钮完整可见。无剩余P0/P1/P2；背景与测试数据差异为预期。

验证：`/tmp/potato-approval-selected-final.xcresult` 3项UI全部通过：收起/重开/拒绝，直接选择永久目录权限并提交，断线与过期禁用（最大辅助字号）。本轮未修改核心审批政策；未发布，未做真机验收。原生SwiftUI场景，无浏览器控制台检查。

## 去冗余后的复验

用户明确批准的文字与密度修订覆盖原始设计图：去掉电脑/会话、重复范围提示，路径合并并加重末级目录，权限缩写、按钮统一“允许”。最终图更新为`trimmed-implementation.png`及`trimmed-persistent.png`（1179×2556，393×852pt @3x），默认360pt面板。两种范围选择状态均已直接查看，整体和正文/按钮区域无截断，颜色/字体沿用，持续授权说明完整且不重复。3条UI回归通过，范围RPC行为保持原有实现。本轮无新增P0/P1/P2，final result: passed。仍未做真机验收。
