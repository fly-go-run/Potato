# 简洁首页与模型弹层验收

来源：用户提供的 Photo 1.jpg 与 Photo 2.jpg（本会话附件）。目标为参考其简洁的信息层级和留白，保留 Potato 原生功能及品牌，不复刻竞品模型名称和系统键盘。

## 第一轮

对照：`home-comparison.png`、`model-comparison.png`，均在同一张对照图中左侧放参考，右侧放原生截图。参考 589×1280，iPhone 17 截图 1206×2622（402×874pt，3×）；统一等比归一到 390px 宽度，不拉伸。系统状态栏和键盘布局随设备变化，不计为应用差异。

- P2：模型只有两项时固定 530pt 弹层产生多余底部留白。改为根据常用条目数计算高度，最大 530pt。
- P2：原生默认列表背景偏灰，层次与参考的白色列表不一致。显式指定白色行底，减少顶部内容边距。
- P2：首页现有品牌图片有方形底边，干扰空白欢迎区。改用圆形裁切保留现有品牌图片。
- P3：勾选原先沿用深色 tint；随本轮改为蓝色。

## 最终复核

第二轮全图对照：`home-comparison-final.png`、`model-comparison-final.png`；原始截图：`simple-home.png`、`simple-home-keyboard.png`、`simple-model-sheet.png`、`simple-thinking-sheet.png`。已打开同图对照及三屏预览 `preview.png` 检查。全图归一后输入框和模型行清晰，无需另做局部放大。

- 字体：保留中文系统字体、22pt 圆体欢迎语和动态字号。正文、次要信息和选中状态层级明确。采用中文和 Potato 内容，与参考英文衬线欢迎语有意不同。
- 间距：欢迎区居中，输入框贴近底部且不遮挡键盘；模型弹层按条目数收紧高度，白色分组之间保留 16pt 间距；二级思考页展开以保证可滚动。
- 颜色：浅色画布、白色输入框/列表、灰色次要信息、蓝色勾选均可区分。第一轮灰底问题已解决。
- 图片：使用既有 PotatoMark，圆形裁切去掉方形边界，清晰无拉伸。与参考产品品牌不同属于明确设计适配。
- 文案：首页只保留一句欢迎语；模型/思考/更多模型层级清楚。截图中的快速/深入模型和 quick/deep 是隔离测试目录，不硬编码为生产模型。
- 小屏：SE 为 375×667pt / 750×1334px，`se-simple-model-sheet.png` 和 `se-local-model-accessibility-keyboard.png` 已打开检查。小屏最大辅助字号优先保证输入及控制按钮，欢迎内容可滚动。没有被遮住的持久操作入口。
- 有意保留：顶部历史、新对话、更多三个既有入口；真实系统键盘、发送和语音行为。这里做参考风格适配，不宣称像素复刻。

## 交互验证

- iPhone 17：本机 5 条与远程 5 条 UI 回归通过，覆盖思考开关、档位、选择持久化、重新生成、草稿保护、搜索返回、远程发送参数、丢失回执重试及大字操作。
- 第一轮远程测试曾因环境开关未进入测试进程而跳过，随后以 `TEST_RUNNER_POTATO_IOS_DRAFT_UI=1` 重跑 5 条全部通过；跳过不计为通过。
- 最终视觉修正后：iPhone 17 的首页/分层选择和远程请求回归 2 条通过；SE 的首页/分层选择与最大辅助字号 2 条通过。最终原生测试构建通过。
- 结果包：`/tmp/potato-simple-ui.xcresult`、`/tmp/potato-simple-remote.xcresult`、`/tmp/potato-simple-final.xcresult`、`/tmp/potato-simple-se-final.xcresult`。截图保存在本目录。
- 验证使用本机可控服务，没有调用真实供应商；本次没有发布 TestFlight。真机、iOS 17 和完整 VoiceOver 人工验收不在这轮验证范围。

第一轮列出的 P2 问题均已修复并复核，无剩余 P0/P1/P2 视觉问题。

final result: passed
